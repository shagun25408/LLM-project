# Run the integrated project

From this project folder, run:

```powershell
npm install
npm run dev
```

Then open `http://localhost:3000`.

The first command installs the liquid-metal shader, Framer Motion, and the small shadcn-style UI dependencies added for the hero.
